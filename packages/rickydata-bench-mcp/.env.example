# rickydata-bench-mcp environment

# Base URL of the public rickydata_bench API. Defaults to production.
BENCH_API_URL=https://benchmarks.rickydata.org

# Max characters per tool response.
RESPONSE_MAX_LENGTH=200000

# Transport: stdio (default) or http
# TRANSPORT=stdio
# PORT=8080
